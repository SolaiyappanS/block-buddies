{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "games": {
      ".read": "auth !== null",
      ".write": "auth !== null",
      "$gameId": {
        ".read": "auth !== null",
        ".write": "auth !== null",
        "players": {
          ".read": "auth !== null",
          ".write": "auth !== null"
        },
        "gameObjects": {
          ".read": "auth !== null",
          ".write": "auth !== null"
        },
        "gameState": {
          ".read": "auth !== null",
          ".write": "auth !== null"
        }
      }
    }
  }
}